# LIRA 关键文件代码逻辑解析

本文档旨在详细梳理 LIRA 项目中几个核心 Python 文件的代码逻辑，并重点说明其中神经网络的使用情况。

---

## 1. `model_probing.py` - 探测模型的核心

该文件定义了 LIRA 框架的“大脑”——一个用于预测分区相关性的神经网络。

### 1.1 神经网络架构: `MLP_2_Input`

LIRA 使用一个特殊设计的多层感知机（MLP），它能同时处理两种不同类型的输入。

*   **类定义**: `class MLP_2_Input(nn.Module)`
*   **核心设计**: 该模型包含两个独立的输入分支，分别处理两种特征，最后将它们融合在一起进行最终预测。

#### 输入 (Inputs):
1.  `x_dist`: **距离向量**。这是一个一维向量，其维度等于总分区数 `n_bkt`。向量中的每一个值，代表了当前查询向量到对应分区中心点的距离。
2.  `x_vec`: **原始查询向量**。这是查询向量本身，维度为 `dim`。

#### 网络结构:
1.  **距离处理分支 (`distance_net`)**:
    *   一个独立的序列神经网络，接收 `x_dist`。
    *   结构: `Linear(input_dim1, 128)` -> `ReLU` -> `Linear(128, 64)` -> `ReLU`。
    *   作用：从距离信息中提取高维特征。

2.  **向量处理分支 (`vector_net`)**:
    *   另一个独立的序列神经网络，接收 `x_vec`。
    *   结构: `Linear(input_dim2, 128)` -> `ReLU` -> `Linear(128, 64)` -> `ReLU`。
    *   作用：从查询向量本身提取高维特征。

3.  **融合与输出 (`fc`)**:
    *   将上述两个分支的输出（两个 64 维的向量）拼接 (`torch.cat`) 成一个 128 维的向量。
    *   将拼接后的向量送入最终的全连接层进行预测。
    *   结构: `Linear(128, 128)` -> `ReLU` -> `Linear(128, output_dim)` -> `Sigmoid`。
    *   **输出 (Output)**: 最终输出一个维度为 `n_bkt` 的向量，其中每个值都在 0 到 1 之间（由 `Sigmoid` 激活函数保证）。这个向量的每一个值，可以被理解为**模型预测的、对应分区包含查询 k-NN 的概率**。

### 1.2 主要函数

*   `model_train`: 标准的 PyTorch 训练函数。它接收训练数据，执行前向传播、计算损失（BCELoss）、反向传播和优化器更新。
*   `model_evaluate`: 在测试集上评估模型性能。除了计算损失，它还会使用一个固定的阈值（`sigma = 0.5`）将概率输出转换为二元的 0/1 预测，并计算准确率、命中率等指标。
*   `model_infer`: 用于推理。它只进行前向传播，返回模型的原始概率输出和基于阈值的二元预测。

---

## 2. `LIRA_smallscale.py` - 总控与流程实现

这是项目的主执行脚本，负责将所有模块串联起来，完成从数据准备到模型训练、再到最终评估的整个流程。

### 神经网络的应用流程

`LIRA_smallscale.py` 中与神经网络相关的逻辑是核心，具体步骤如下：

1.  **加载数据与初始分区** (`main` 块):
    *   调用 `utils.load_data` 加载数据集。
    *   调用 `utils.build_kmeans_index` 对数据进行 K-Means 聚类，获得初始的分区 (`data_2_bkt`) 和簇中心 (`kmeans.centroids`)。

2.  **准备训练数据** (`main` 块):
    *   **计算真实标签 (Labels)**:
        *   首先用 `utils.get_flat_GT_knn` 通过暴力搜索得到每个数据/查询点的真实 k-NN。
        *   然后调用 `utils.get_knn_distr_redundancy`，根据 k-NN 的实际分布，为每个数据/查询点生成一个 `n_bkt` 维的**标签向量** (`labels_data`, `labels_query`)。如果分区 `j` 包含了 k-NN，则向量的第 `j` 位为 1，否则为 0。
    *   **准备输入特征 (Inputs)**:
        *   **距离向量 (`distances_data_scaled`)**: 调用 `utils.get_scaled_dist` 函数，计算每个数据/查询点到所有 `n_bkt` 个簇中心的距离，并进行标准化处理。这对应 `MLP_2_Input` 的 `x_dist` 输入。
        *   **原始向量 (`x_d`, `x_q`)**: 数据/查询向量本身。这对应 `MLP_2_Input` 的 `x_vec` 输入。
    *   **创建 DataLoader**: 将准备好的输入特征和标签封装成 PyTorch 的 `DataLoader`，用于分批次训练。

3.  **模型训练与评估** (`main` 块):
    *   实例化 `model_probing.MLP_2_Input` 模型。
    *   定义损失函数 (`nn.BCELoss`) 和优化器 (`Adam`)。
    *   在一个循环中，重复调用 `model_probing.model_train` 进行训练和 `model_probing.model_evaluate` 进行评估，并用 `cal_metrics` 记录性能指标。

4.  **应用模型进行冗余和查询** (`main` 块):
    *   **学习型冗余 (`mul_partition_by_model`)**:
        *   模型训练完毕后，将其应用于训练集自身，得到每个数据点应被探测的分区概率。
        *   对于那些模型认为难以判断的点，此函数会根据模型的预测，将该点“复制”到多个高概率的分区中，从而实现数据冗余，解决边界问题。
    *   **查询调优 (`query_tuning`)**:
        *   这是 LIRA 性能验证的关键。该函数接收模型对查询集的预测概率 (`all_outputs`)。
        *   它会遍历一系列不同的概率阈值（从 0.1 到 1.0）。在每个阈值下，它模拟一次查询过程：只搜索那些模型预测概率高于当前阈值的分区。
        *   最后，它会记录在不同阈值下的召回率（Recall）和计算开销（Computations），从而可以绘制出性能曲线，找到最佳的平衡点。

---

## 3. `utils.py` - 数据处理与辅助工具

该文件包含一系列支持主流程的工具函数。

### 与神经网络相关的关键函数

*   `load_data`: 加载 ANN 基准测试数据集。
*   `get_flat_GT_knn`: **标签生成的基础**。通过暴力搜索计算真实的 k-NN，为监督学习提供真值（Ground Truth）。
*   `get_knn_distr_redundancy`: **标签生成的核心**。根据 `get_flat_GT_knn` 的结果，统计 k-NN 在各个分区中的分布情况，最终生成用于训练的二元标签向量。
*   `get_scaled_dist`: **特征工程的核心**。计算查询/数据点到所有簇中心的距离，并使用 `StandardScaler` 进行归一化。归一化是保证神经网络稳定训练的关键步骤。
*   `build_kmeans_index`: 进行 K-Means 聚类，其产出的簇中心点是计算距离特征所必需的。
*   `create_inner_indexes`: 根据分区结果，为每个分区建立独立的 Faiss 索引（如 HNSW 或 FLAT），用于最终的搜索阶段。

---

## 4. `LIRA_largescale.py` - 大规模数据集版本

这个文件在逻辑上与 `LIRA_smallscale.py` 非常相似，主要区别在于为了适应更大规模的数据集，做出了一些策略上的调整：

*   **模型训练**: 探测模型是在一个较小的数据采样（例如，1% 的数据）上进行训练的，而不是整个数据集，以节省时间和计算资源。
*   **数据冗余**: 采用了更充分的冗余策略（例如，所有数据都被复制一次），以应对大规模数据集下更复杂的数据分布。

这些调整使得 LIRA 框架能够被扩展到处理数十亿级别的大规模向量数据集。
