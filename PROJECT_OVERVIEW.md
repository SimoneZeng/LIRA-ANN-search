# LIRA 项目概述

本项目实现了一个名为 **LIRA (Learning-based Query-aware Partition Framework)** 的高级近似最近邻（ANN）搜索框架。LIRA 旨在提升基于分区（Partition-based）的 ANN 搜索方法的效率和准确性。

## 1. 背景与问题

基于分区的 ANN 方法（如 IVF）是处理大规模向量搜索的常用技术。它们通过以下方式工作：

1.  **建立索引**：将数据集通过聚类算法（如 K-Means）划分为多个分区（或称为桶/簇）。
2.  **查询**：在查询时，首先确定与查询向量最近的 `nprobe` 个分区，然后仅在这些分区内进行搜索，从而避免了对整个数据集的扫描。

然而，传统方法存在两个主要问题：

*   **无效的分区探测**：传统方法仅根据查询向量与分区中心点的“距离”来选择要探测的分区。这种策略没有考虑数据的实际分布，常常导致计算资源被浪费在那些并不包含真正最近邻（k-NN）的分区上。
*   **边界问题 (Boundary Problem)**：一个查询的 k-NN 可能会因为硬性的分区边界而被分割到多个不同的分区中。这导致了 k-NN 的“长尾分布”现象，使得为了保证高召回率，不得不设置一个较大的 `nprobe` 值，从而降低了搜索效率。

## 2. LIRA 的核心思想

LIRA 框架通过引入机器学习模型来解决上述问题，其核心思想可以分为两个部分：

*   **智能探测 (Learning to Probe)**：LIRA 训练一个“探测模型”（一个神经网络），该模型能够根据查询向量的特征，更智能地预测哪些分区“最有可能”包含其 k-NN。搜索时不再依赖于简单的距离排序，而是依赖于模型的预测概率，从而实现更精准、高效的分区选择。

*   **冗余优化 (Learning-based Redundancy)**：为了解决“边界问题”，LIRA 使用训练好的探测模型来识别那些位于分区边界、难以归类的“模糊”数据点。然后，它会将这些数据点复制到由模型预测出的多个相关分区中。这种“学习型”的数据冗余策略，有效地缓解了 k-NN 的长尾分布，使得在搜索时可以用更小的 `nprobe` 值达到同样甚至更高的召回率。

## 3. LIRA 工作流程

LIRA 的整个工作流程可以概括为以下几个步骤：

1.  **初始分区**：
    *   使用标准的 K-Means 算法对整个数据集进行初始分区，得到一系列的簇中心点。

2.  **探测模型训练**：
    *   **准备训练数据**：
        *   **特征 (Inputs)**：对于数据集中的每个向量，准备两组特征：1) 该向量本身；2) 该向量到所有簇中心点的距离向量。
        *   **标签 (Labels)**：通过暴力搜索计算出每个向量的真实 k-NN，然后确定这些 k-NN 分布在哪些分区中。如果一个分区包含了至少一个 k-NN，则该分区对应的标签为 1，否则为 0。
    *   **模型训练**：
        *   使用上述特征和标签，训练一个二分类神经网络（在本项目中为 `MLP_2_Input`）。该网络的目标是学习预测对于任意输入向量，每个分区包含其 k-NN 的概率。

3.  **学习型冗余分区**：
    *   将训练好的模型应用于数据集中的所有点。
    *   对于模型认为难以分类的点（即，其 k-NN 可能分布在多个分区中的点），根据模型的预测结果，将这些点复制到多个最相关的分区中，从而形成一个经过优化的、带有冗余的索引结构。

4.  **查询与搜索**：
    *   当一个新的查询到来时，同样将其输入到训练好的探测模型中。
    *   模型会为所有分区输出一个概率分数。
    *   LIRA 选择探测概率分数最高的若干个分区进行搜索，而不是距离最近的几个。
    *   通过 `query_tuning` 函数，可以测试不同概率阈值下的召-效率权衡，从而为实际应用找到最佳的操作点。

通过以上流程，LIRA 能够构建一个更智能、更高效的 ANN 索引，显著提升大规模向量检索的性能。
